.cosmetics-show-wrap
  .container
    .cosmetic-name
      = @cosmetic.name
    .brand-name
      = @brand.name
    .category-name
      = @category.name
    .ingredients-wrap
      - if @ingredients.present?
        %ul
          - @ingredients.each do |ingredient|
            %li
              = ingredient.name
