.header-wrap
  .container
    %ul.navbar
      %li.title
        = link_to "Skiny", root_path
      %li.user-nav
        - if signed_in?
          = link_to "マイページ", user_path(current_user)
        - else
          = link_to "ログイン", signin_path
